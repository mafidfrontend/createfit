<template><div class="rounded-xl border border-line bg-white p-4"><div class="flex justify-between text-sm"><span class="text-ink/55">Изделие</span><b>{{ formatUsd(productPrice) }}</b></div><div class="mt-2 flex justify-between text-sm text-ink/55"><span>Ткань</span><b>+{{ formatUsd(fabricPrice) }}</b></div><div class="mt-2 flex justify-between text-sm text-ink/55"><span>Доставка</span><b>+{{ formatUsd(deliveryPrice) }}</b></div><div class="mt-4 flex justify-between border-t border-line pt-3 text-base font-bold"><span>Итого</span><span class="text-sage">{{ formatUsd(total) }}</span></div></div></template>
<script setup lang="ts">import { formatUsd } from '~/utils/pricing'; defineProps<{ productPrice: number; fabricPrice: number; deliveryPrice: number; total: number }>()</script>
